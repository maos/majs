#ifndef _FREQUENCY_H_
#define _FREQUENCY_H_

#include <vector>
#include <string>

std::vector< int > frequency( const std::string& s );

// a and b are frequency arrays
double euclid_metric( const std::vector< int >& a, const std::vector< int >& b);

// also try to play with minkowski metric

#endif // _FREQUENCY_H_
